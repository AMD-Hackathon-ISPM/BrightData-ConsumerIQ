apiVersion: v1
kind: Service
metadata:
  name: consumeriq-embeddings
  namespace: consumeriq
spec:
  type: ClusterIP
  selector:
    app: consumeriq-embeddings
  ports:
    - name: http
      port: 8080
      targetPort: 8080
