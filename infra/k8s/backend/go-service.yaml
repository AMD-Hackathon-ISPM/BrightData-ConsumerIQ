apiVersion: v1
kind: Service
metadata:
  name: consumeriq-go
  namespace: consumeriq
spec:
  type: ClusterIP
  selector:
    app: consumeriq-go
  ports:
    - name: http
      port: 8080
      targetPort: 8080
