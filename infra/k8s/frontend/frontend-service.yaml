apiVersion: v1
kind: Service
metadata:
  name: consumeriq-frontend
  namespace: consumeriq
spec:
  type: ClusterIP
  selector:
    app: consumeriq-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
