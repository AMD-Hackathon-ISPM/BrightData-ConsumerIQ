apiVersion: v1
kind: Service
metadata:
  name: consumeriq-nginx
  namespace: consumeriq
spec:
  type: NodePort
  selector:
    app: consumeriq-nginx
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
